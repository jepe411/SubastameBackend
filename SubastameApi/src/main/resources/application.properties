## default connection pool
spring.datasource.hikari.connectionTimeout=20000
spring.datasource.hikari.maximumPoolSize=5

## PostgreSQL
spring.datasource.url=jdbc:postgresql://localhost:5432/db_subasta
spring.datasource.username=db_subasta_user
spring.datasource.password=db_subasta_user

#drop n create table again, good for testing, comment this in production standard Hibernate property values are none, validate, update, create, and create-drop. 
spring.jpa.hibernate.ddl-auto=validate
hibernate.temp.use_jdbc_metadata_defaults false
spring.jpa.properties.hibernate.temp.use_jdbc_metadata_defaults=false
